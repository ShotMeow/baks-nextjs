"use client";
import type { FC } from "react";
import Slider from "./Slider";

const Hero: FC = () => {
  return (
    <section className="my-10 overflow-x-hidden">
      <Slider />
    </section>
  );
};

export default Hero;
